mutation forgotPassword(
    $email: String!
) {
    forgotPassword(
        email: $email
    ) {
        errors{
            message
            field
            details
        }
        success
    }
}