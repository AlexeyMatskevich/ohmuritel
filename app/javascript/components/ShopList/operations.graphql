query PRODUCTS {
    products{
        id,
        name,
        previewDescription,
        price,
        imageUrl
    }
}

query canEdit {
    currentUser {
        canCreateProduct {
            value
        }
    }
}
