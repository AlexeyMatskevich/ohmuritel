query PRODUCTS {
    products{
        id,
        name,
        previewDescription,
        price
    }
}

query canEdit {
    currentUser {
        canCreateProduct {
            value
        }
    }
}
